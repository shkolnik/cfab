# Bake-off shared fixes

Fixes made on `embedded` that `main` (FRR) also needs; James applies them after the bake-off decision.

The fallback segment was built under the name `rescue` and renamed at merge; its commits on the
branch are still titled `rescue segment (task N)`, everything below uses the shipped name.

| commit | what | main needs |
|---|---|---|
| (none yet) | `.github/workflows/release.yml` still builds `--target x86_64-unknown-linux-musl` with no holo clone/apt/`holo-check` step; breaks if `embedded` is merged or tagged | rework release.yml for whichever engine wins (musl static size is a post-decision measurement, spec §11) |
| (none yet) | `src/derive.rs:299/:307` fail `cargo fmt --check` (CI Format step red on both branches) | `cargo fmt` — same bytes on main |
| `engine_ctl: up names an ospf interface the engine still reports down` | `up` re-reads the state document for a 3 s settle and then warns by name about every configured OSPF interface still in state `down` (gate-0 evidence §8.4: `up` was silent about a wire no hello leaves); it warns and completes rather than refusing, because one dead wire must not cost an availability-first host its whole fabric. `waiting` is not down — a passive identity interface never leaves it | same hole, different mechanism: main's `up` readback only text-matches `vtysh show running-config`, which echoes an `interface` block whether or not the interface is operationally up, so a segment whose wire has no carrier at `up` time passes silently. main needs its own operational read (`show ip ospf <id> interface`) and the same warn-don't-refuse posture. Neither branch is exposed to the *absent*-interface form — `mk_vlan`/`mk_identity` use `run_ok`, so a missing wire fails `up` before readback |
| `foreign forward-hook drops: report them, then neutralize them` | A foreign stack's forward-hook base chain with policy `drop` kills transit that cfab accepts, and cfab cannot out-accept it — every base chain at the hook runs and one drop verdict ends the packet. Measured with Docker on pve1 2026-09-04: transit 5/5 → 0/5 while `verify` printed `posture ok`. `verify` and the watchdog now report any uncovered foreign forward-hook drop, and `up`/the watchdog install a tagged accept in `DOCKER-USER` (removed by `down`) | **identical hole on main** — the defect is in `verify`/`fwd-watchdog`/`up`/`down`, none of which is engine-specific. The whole change is in `src/commands/common.rs` plus four call sites and applies to main unchanged |
| `fallback segment (task 1): Island::Any, Role::Fallback, model validation` | `src/model.rs` gains `Island::Any` (`Member::wire` returns `None` for it by an explicit arm, never an index), `Role::Fallback`, and five `Fabric::validate` checks: island `any` ⇔ role `fallback` both directions, fallback `ifname` ≤ 12 chars (slaves are `<ifname>-<island>`, IFNAMSIZ 15), fallback cost strictly between its own zone's longest host path (sum of that zone's non-fallback class-row costs) and `LEAF_COST_OFFSET`, and a `ZONE_TABLE` gw island of `any` refused (the ingress leg does not fan out yet). `examples/fabric.conf` gains three fallback rows (`cfab-st-fb`/`cfab-cl-fb`/`cfab-mg-fb`, cost 5000 each) and `cfab check` now reports fallback legs separately from class segments. This task is model-only — no derive/up/verify/engine consumer exists yet, so fallback rows are inert beyond validation | **engine-independent, main needs it unchanged** — none of `model.rs` or `main.rs`'s `check` summary is engine-specific; `main` should take the same model/example/check changes verbatim once the branches are reconciled |
| `fallback segment (task 2): FallbackRow, fallback_rows(), home wire, zone_ifs/owned_forwarding` | `src/derive.rs` gains `Slave`/`FallbackRow` and `View::fallback_rows()`: each `any` CLASS_TABLE row fans out over the member's wires in st/cl/mg order (one slave per wire, named `<ifname>-<island>`), homed on the wire carrying that zone's cheapest class segment this member has (derived by walking `class_rows()`, never declared; ties keep the first row in CLASS_TABLE order). `zone_ifs` gains the fallback ifname after segments, before the ingress leg; `owned_forwarding` gains each fallback bond as `transit` (a segment) and each slave as `false` (L2 only). `class_rows_of`/`wires()`/`segments_of` are unchanged by construction (`Member::wire(Island::Any)` is always `None`, from task 1) — a new test asserts the bond and its slaves never appear in `wires()` or `segments_of`. Note for the task that owns `src/commands/policy_teeth.rs`: `first_if`/`second_if` there read `fabric.class_table` unfiltered by role and rely on the three fallback rows being declared last; this task did not reorder `class_table` or add rows, so that ordering assumption is unchanged, not fixed | **engine-independent, main needs it unchanged** — `derive.rs` has no engine-specific code; `main` should take the same `FallbackRow`/`fallback_rows()`/`zone_ifs`/`owned_forwarding` changes verbatim |
| `fallback segment (task 3): up and down build and tear down the bond and its slaves` | `src/commands/up.rs` gains `mk_fallback`: per fallback row an `ip link add <bond> type bond mode active-backup miimon 100 num_grat_arp 3 updelay 0 fail_over_mac none` (refuse-unless-`bond` on re-run), each slave created DOWN and address-less by an extended `mk_vlan(…, addr: Option<&str>, bring_up: bool, …)` with the PCP map on the slave, enslaved only when it is not already in that bond (re-issuing `master` is EBUSY; a slave found enslaved to a FOREIGN bond is REFUSED rather than handed to the kernel for a bare EBUSY), each slave then written `forwarding=0` explicitly (a slave inherits conf/default, which a leaf's external kernel owner may keep at 1), then `primary <home slave> primary_reselect always` set AFTER the slaves exist (at `add` time it is a silent no-op), then the address, `class_sysctls` and the bond up. `enable_forwarding` gains the bonds (never the slaves) so `up` does not end DEGRADED against `owned_forwarding()`. The `up` settle warning renders a down bond as `<zone>/<bond> (no wire with carrier under it)` instead of a bare interface name, and joins the list with `, `. `src/commands/down.rs` deletes the bonds first (kind ` bond ` or REFUSE) and then their slaves (kind ` vlan ` or REFUSE) — `ip link del <bond>` releases its slaves, it does not delete them. The bond parameters are five named constants carrying the spike measurement and the Task 7 sweep note. Existing class-row and gw-leg argv are byte-identical (proved by diffing the full recorded call sequence of `up` for a host and a leaf before and after) | **engine-independent, main needs it unchanged** — `up.rs`/`down.rs` here speak only `ip`, `/proc` and `/sys`; nothing touches the routing engine. `main` should take `mk_fallback`, the `mk_vlan` signature change, the forwarding loop, the settle-warning wording and the `down` loops verbatim |
| `fallback segment (task 5): verify's fallback clause and the island-disjoint expectation` | `src/commands/verify.rs` gains a `fallback()` clause that runs after convergence: per zone it reads `/sys/class/net/<bond>/bonding/{mii_status,active_slave}` and says `fallback <zone> via <wire>` (the WIRE, mapped back through `FallbackRow.slaves`), degrading with `fallback <zone> via <wire> (home <home> has carrier but is not active)` only while the home wire still has carrier and with `fallback <zone> no carrier` when the bond is down; counts the peers carrying that zone's fallback row that are at least 2-Way on the bond in the engine state document (`fallback <zone> neighbors: <n>/<n> 2-Way or better`, and a peer below the bar is NAMED — 2-Way, not Full, because two DROthers never go further on a broadcast LAN); and reports an island-disjoint peer as `<zone> id .0.<n> (<peer>) via fallback <bond>`. `check()`'s peer-reachability loop now expects that same bond when the two members share no class segment in the zone — before, `prim` was the empty string and an island-disjoint peer reached over the bond never converged (`not primary `, proved by regressing it). The rp_filter and leaf-forwarding posture loops gain the bonds and never the slaves (L2 only). A new `Ctx.fallback` list keeps fallback conditions out of the gw note; the headline `<n> BFD up` is untouched — fallback carries no BFD | **engine-independent, main needs it unchanged** — `verify.rs` here reads only `ip`, `/proc`, `/sys` and the engine's state document through the existing `engine_ctl::state`; the neighbor states are ietf-ospf enum values, which FRR's JSON also reports. `main` should take the `fallback()` clause, the `check()` expectation and the posture-loop change verbatim |
| `fallback segment (task 6): policy, mark, fwd-watchdog proved; policy-teeth role guard` | `src/emit/policy.rs` and `src/emit/mark.rs` were unchanged (both already read `View::zone_ifs()`, which Task 2 already carries the fallback bond in); this task adds golden tests proving the bond lands in the zone's forward-policy set and the `cfab` owned set (never a slave in the zone set — a slave carries no zone traffic of its own) and in the PCP/DSCP marking rules (the on-wire PCP itself comes from the active slave's egress-qos-map, set by `up`, not by a mark rule — INFERRED here, hardware-confirmed in a later task). `src/commands/fwd_watchdog.rs` gains two tests proving `owned_forwarding()` (Task 2) already flags the bond `transit`-eligible like a segment and a slave never. `src/commands/policy_teeth.rs` fixes the ordering assumption Task 2 flagged: `first_if`/`second_if` now explicitly filter out `Role::Fallback` instead of relying on fallback rows sorting last in `CLASS_TABLE`, and the fixture's veth set now includes the fallback bond ifnames (never slaves) so the generated ruleset is exercised against them too — `policy_teeth` needs `CAP_NET_ADMIN` and does not run in this cockpit, so this is unverified beyond compiling; the container proof (a later hardware task) is the only proof of actual fallback transit | **engine-independent, main needs it unchanged** — none of `policy.rs`/`mark.rs`/`fwd_watchdog.rs`/`policy_teeth.rs` is engine-specific; `main` should take the tests and the `policy_teeth.rs` role guard verbatim |
| `fallback segment (task 9): the ingress leg migrates too` | James ruled 2026-09-04 that a single MIGRATING first gw leg replaces the previously approved "second gw leg on the fallback island" — one leg, one BGP session, island-independent. `src/model.rs` drops task 1's temporary refusal of a `ZONE_TABLE` gw island of `any` and gains a shared `bond_ifname_too_long`/`MAX_BOND_IFNAME` predicate used by both the fallback ifname check and a new derived-ingress-bond-name check. `src/derive.rs` renames `GwRow.wire` to `home` and adds `GwRow.slaves` + `GwRow::migrates()`, so a gw row has exactly `FallbackRow`'s shape: on a physical island `slaves` is empty and the leg is the same plain sub-interface it has always been; on island `any` the leg fans out through the extracted `slaves_of()` helper (shared with `fallback_rows_of`) into `cfab-gw<id>` over `cfab-gw<id>-<island>`, homed on the wire carrying that zone's cheapest segment (falling back to the first wire when the zone has an ingress but no segment on this member, rather than dropping the leg). `owned_forwarding` gains the slaves as `false`. `src/commands/up.rs` generalizes `mk_fallback` into `mk_bond_leg(&BondLeg{…})`, called by both legs; `src/commands/down.rs` tears a migrating leg down as a bond (bond first, then slaves) instead of handing it to the plain sub-interface loop, which would REFUSE it "neither macvlan nor vlan" and strand it; the `up` settle warning names a down migrating leg with the same "no wire with carrier under it" wording. `emit/engine.rs` is untouched — the leg keeps its name and stays passive in the zone's OSPF instance, and no BGP exists on this branch. `examples/fabric.conf` KEEPS `mg:249:` (the rack precondition — VID 249 tagged on every island's ports, storm control first — is not met) and documents `any` in the gw column. Byte-identical for an island gw row: the full recorded `up` and `down` call sequences for all three members are unchanged, diffed before and after | **engine-independent, main needs it unchanged** — `model.rs`/`derive.rs`/`up.rs`/`down.rs` here speak only `ip`, `/proc` and the declaration; `main` should take the `GwRow` shape, `slaves_of`, `mk_bond_leg` and the `down` partition verbatim. Note this commit also incidentally fixes the `src/derive.rs` `cargo fmt --check` failure listed above (the offending lines were inside `gw_rows_of`, which this task rewrote) |
| `fallback segment: updelay 0 -> 500, the measured value` | `FALLBACK_UPDELAY_MS` in `src/commands/up.rs` moves from the spike's `0` to the swept value `500`, and its doc comment now records the value as MEASURED rather than a target. Sweep (n=1 per value, container fixture on pve3): return-cost window 0.074 / 0.277 / 0.574 s at 0 / 200 / 500 with zero packets lost at every value; 20 / 8 / 2 active-slave migrations over ten 250 ms flaps; failover AWAY from a dead wire 0.026-0.042 s at every value, so `updelay` gates only the reselect and can never lengthen an outage. The whole cost is a sub-second window in which `verify` reads DEGRADED after a legitimate return; the benefit is 10x fewer GARP bursts and MAC moves on a bouncing wire | **engine-independent, main needs it unchanged** — one constant and two pinned-argv tests in `up.rs`, which speaks only `ip` |
| `fallback segment (task 8): switch side and docs` | `examples/fabric.conf` records the second half of the switch precondition: a fallback vid must be tagged on every island's access ports AND end to end across the inter-switch links, because one fallback vid is one broadcast domain spanning every island and a per-island fragment leaves the bond's carrier check passing on a slave whose tag reaches nobody. The rest of this task lands outside this crate, in the shell prototype of the same declaration (a separate implementation reading the same table, which learns island `any` and role `fallback` with the same five validation rules `cfab check` applies, drops fallback rows from its wire-semantics row resolver by construction, gains the fallback and gw-slave fan-outs, carries the bond in its zone interface set, and — in its switch-VLAN generator, the list a human configures the physical switches from — prints the fallback VIDs on every island, the backbone requirement and the storm-control precondition, all of which an `any` row was SILENTLY absent from before) | **engine-independent** — a comment in the shipped example; `main` should take it verbatim |
