# Bake-off shared fixes

Fixes made on `embedded` that `main` (FRR) also needs; James applies them after the bake-off decision.

| commit | what | main needs |
|---|---|---|
| (none yet) | `.github/workflows/release.yml` still builds `--target x86_64-unknown-linux-musl` with no holo clone/apt/`holo-check` step; breaks if `embedded` is merged or tagged | rework release.yml for whichever engine wins (musl static size is a post-decision measurement, spec §11) |
| (none yet) | `src/derive.rs:299/:307` fail `cargo fmt --check` (CI Format step red on both branches) | `cargo fmt` — same bytes on main |
| `engine_ctl: up names an ospf interface the engine still reports down` | `up` re-reads the state document for a 3 s settle and then warns by name about every configured OSPF interface still in state `down` (gate-0 evidence §8.4: `up` was silent about a wire no hello leaves); it warns and completes rather than refusing, because one dead wire must not cost an availability-first host its whole fabric. `waiting` is not down — a passive identity interface never leaves it | same hole, different mechanism: main's `up` readback only text-matches `vtysh show running-config`, which echoes an `interface` block whether or not the interface is operationally up, so a segment whose wire has no carrier at `up` time passes silently. main needs its own operational read (`show ip ospf <id> interface`) and the same warn-don't-refuse posture. Neither branch is exposed to the *absent*-interface form — `mk_vlan`/`mk_identity` use `run_ok`, so a missing wire fails `up` before readback |
| `foreign forward-hook drops: report them, then neutralize them` | A foreign stack's forward-hook base chain with policy `drop` kills transit that cfab accepts, and cfab cannot out-accept it — every base chain at the hook runs and one drop verdict ends the packet. Measured with Docker on pve1 2026-09-04: transit 5/5 → 0/5 while `verify` printed `posture ok`. `verify` and the watchdog now report any uncovered foreign forward-hook drop, and `up`/the watchdog install a tagged accept in `DOCKER-USER` (removed by `down`) | **identical hole on main** — the defect is in `verify`/`fwd-watchdog`/`up`/`down`, none of which is engine-specific. The whole change is in `src/commands/common.rs` plus four call sites and applies to main unchanged |
| `rescue segment (task 1): Island::Any, Role::Rescue, model validation` | `src/model.rs` gains `Island::Any` (`Member::wire` returns `None` for it by an explicit arm, never an index), `Role::Rescue`, and five `Fabric::validate` checks: island `any` ⇔ role `rescue` both directions, rescue `ifname` ≤ 12 chars (slaves are `<ifname>-<island>`, IFNAMSIZ 15), rescue cost strictly between its own zone's longest host path (sum of that zone's non-rescue class-row costs) and `LEAF_COST_OFFSET`, and a `ZONE_TABLE` gw island of `any` refused (the ingress leg does not fan out yet). `examples/fabric.conf` gains three rescue rows (`cfab-st-rs`/`cfab-cl-rs`/`cfab-mg-rs`, cost 5000 each) and `cfab check` now reports rescue legs separately from class segments. This task is model-only — no derive/up/verify/engine consumer exists yet, so rescue rows are inert beyond validation | **engine-independent, main needs it unchanged** — none of `model.rs` or `main.rs`'s `check` summary is engine-specific; `main` should take the same model/example/check changes verbatim once the branches are reconciled |
| `rescue segment (task 2): RescueRow, rescue_rows(), home wire, zone_ifs/owned_forwarding` | `src/derive.rs` gains `Slave`/`RescueRow` and `View::rescue_rows()`: each `any` CLASS_TABLE row fans out over the member's wires in st/cl/mg order (one slave per wire, named `<ifname>-<island>`), homed on the wire carrying that zone's cheapest class segment this member has (derived by walking `class_rows()`, never declared; ties keep the first row in CLASS_TABLE order). `zone_ifs` gains the rescue ifname after segments, before the ingress leg; `owned_forwarding` gains each rescue bond as `transit` (a segment) and each slave as `false` (L2 only). `class_rows_of`/`wires()`/`segments_of` are unchanged by construction (`Member::wire(Island::Any)` is always `None`, from task 1) — a new test asserts the bond and its slaves never appear in `wires()` or `segments_of`. Note for the task that owns `src/commands/policy_teeth.rs`: `first_if`/`second_if` there read `fabric.class_table` unfiltered by role and rely on the three rescue rows being declared last; this task did not reorder `class_table` or add rows, so that ordering assumption is unchanged, not fixed | **engine-independent, main needs it unchanged** — `derive.rs` has no engine-specific code; `main` should take the same `RescueRow`/`rescue_rows()`/`zone_ifs`/`owned_forwarding` changes verbatim |
| `rescue segment (task 3): up and down build and tear down the bond and its slaves` | `src/commands/up.rs` gains `mk_rescue`: per rescue row an `ip link add <bond> type bond mode active-backup miimon 100 num_grat_arp 3 updelay 0 fail_over_mac none` (refuse-unless-`bond` on re-run), each slave created DOWN and address-less by an extended `mk_vlan(…, addr: Option<&str>, bring_up: bool, …)` with the PCP map on the slave, enslaved only when it is not already in that bond (re-issuing `master` is EBUSY; a slave found enslaved to a FOREIGN bond is REFUSED rather than handed to the kernel for a bare EBUSY), each slave then written `forwarding=0` explicitly (a slave inherits conf/default, which a leaf's external kernel owner may keep at 1), then `primary <home slave> primary_reselect always` set AFTER the slaves exist (at `add` time it is a silent no-op), then the address, `class_sysctls` and the bond up. `enable_forwarding` gains the bonds (never the slaves) so `up` does not end DEGRADED against `owned_forwarding()`. The `up` settle warning renders a down bond as `<zone>/<bond> (no wire with carrier under it)` instead of a bare interface name, and joins the list with `, `. `src/commands/down.rs` deletes the bonds first (kind ` bond ` or REFUSE) and then their slaves (kind ` vlan ` or REFUSE) — `ip link del <bond>` releases its slaves, it does not delete them. The bond parameters are five named constants carrying the spike measurement and the Task 7 sweep note. Existing class-row and gw-leg argv are byte-identical (proved by diffing the full recorded call sequence of `up` for a host and a leaf before and after) | **engine-independent, main needs it unchanged** — `up.rs`/`down.rs` here speak only `ip`, `/proc` and `/sys`; nothing touches the routing engine. `main` should take `mk_rescue`, the `mk_vlan` signature change, the forwarding loop, the settle-warning wording and the `down` loops verbatim |
| `rescue segment (task 5): verify's rescue clause and the island-disjoint expectation` | `src/commands/verify.rs` gains a `rescue()` clause that runs after convergence: per zone it reads `/sys/class/net/<bond>/bonding/{mii_status,active_slave}` and says `rescue <zone> via <wire>` (the WIRE, mapped back through `RescueRow.slaves`), degrading with `rescue <zone> via <wire> (home <home> has carrier but is not active)` only while the home wire still has carrier and with `rescue <zone> no carrier` when the bond is down; counts the peers carrying that zone's rescue row that are at least 2-Way on the bond in the engine state document (`rescue <zone> neighbors: <n>/<n> 2-Way or better`, and a peer below the bar is NAMED — 2-Way, not Full, because two DROthers never go further on a broadcast LAN); and reports an island-disjoint peer as `<zone> id .0.<n> (<peer>) via rescue <bond>`. `check()`'s peer-reachability loop now expects that same bond when the two members share no class segment in the zone — before, `prim` was the empty string and an island-disjoint peer reached over the bond never converged (`not primary `, proved by regressing it). The rp_filter and leaf-forwarding posture loops gain the bonds and never the slaves (L2 only). A new `Ctx.rescue` list keeps rescue conditions out of the gw note; the headline `<n> BFD up` is untouched — rescue carries no BFD | **engine-independent, main needs it unchanged** — `verify.rs` here reads only `ip`, `/proc`, `/sys` and the engine's state document through the existing `engine_ctl::state`; the neighbor states are ietf-ospf enum values, which FRR's JSON also reports. `main` should take the `rescue()` clause, the `check()` expectation and the posture-loop change verbatim |
