# Bake-off shared fixes

Fixes made on `embedded` that `main` (FRR) also needs; James applies them after the bake-off decision.

| commit | what | main needs |
|---|---|---|
| (none yet) | `.github/workflows/release.yml` still builds `--target x86_64-unknown-linux-musl` with no holo clone/apt/`holo-check` step; breaks if `embedded` is merged or tagged | rework release.yml for whichever engine wins (musl static size is a post-decision measurement, spec §11) |
| (none yet) | `src/derive.rs:299/:307` fail `cargo fmt --check` (CI Format step red on both branches) | `cargo fmt` — same bytes on main |
| `engine_ctl: up names an ospf interface the engine still reports down` | `up` re-reads the state document for a 3 s settle and then warns by name about every configured OSPF interface still in state `down` (gate-0 evidence §8.4: `up` was silent about a wire no hello leaves); it warns and completes rather than refusing, because one dead wire must not cost an availability-first host its whole fabric. `waiting` is not down — a passive identity interface never leaves it | same hole, different mechanism: main's `up` readback only text-matches `vtysh show running-config`, which echoes an `interface` block whether or not the interface is operationally up, so a segment whose wire has no carrier at `up` time passes silently. main needs its own operational read (`show ip ospf <id> interface`) and the same warn-don't-refuse posture. Neither branch is exposed to the *absent*-interface form — `mk_vlan`/`mk_identity` use `run_ok`, so a missing wire fails `up` before readback |
| `foreign forward-hook drops: report them, then neutralize them` | A foreign stack's forward-hook base chain with policy `drop` kills transit that cfab accepts, and cfab cannot out-accept it — every base chain at the hook runs and one drop verdict ends the packet. Measured with Docker on pve1 2026-09-04: transit 5/5 → 0/5 while `verify` printed `posture ok`. `verify` and the watchdog now report any uncovered foreign forward-hook drop, and `up`/the watchdog install a tagged accept in `DOCKER-USER` (removed by `down`) | **identical hole on main** — the defect is in `verify`/`fwd-watchdog`/`up`/`down`, none of which is engine-specific. The whole change is in `src/commands/common.rs` plus four call sites and applies to main unchanged |
| `rescue segment (task 1): Island::Any, Role::Rescue, model validation` | `src/model.rs` gains `Island::Any` (`Member::wire` returns `None` for it by an explicit arm, never an index), `Role::Rescue`, and five `Fabric::validate` checks: island `any` ⇔ role `rescue` both directions, rescue `ifname` ≤ 12 chars (slaves are `<ifname>-<island>`, IFNAMSIZ 15), rescue cost strictly between its own zone's longest host path (sum of that zone's non-rescue class-row costs) and `LEAF_COST_OFFSET`, and a `ZONE_TABLE` gw island of `any` refused (the ingress leg does not fan out yet). `examples/fabric.conf` gains three rescue rows (`cfab-st-rs`/`cfab-cl-rs`/`cfab-mg-rs`, cost 5000 each) and `cfab check` now reports rescue legs separately from class segments. This task is model-only — no derive/up/verify/engine consumer exists yet, so rescue rows are inert beyond validation | **engine-independent, main needs it unchanged** — none of `model.rs` or `main.rs`'s `check` summary is engine-specific; `main` should take the same model/example/check changes verbatim once the branches are reconciled |
