# {{ ansible_managed }}
# /etc/default/cfab — environment for cfab.service (read via EnvironmentFile=).
# CFAB_HOST names the [[member]] row this host runs as; the packaged unit reads nothing else.
CFAB_HOST={{ cfab_host }}
