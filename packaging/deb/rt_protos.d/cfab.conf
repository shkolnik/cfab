# cfab kernel route-protocol names (iproute2 rt_protos.d), matching src/emit/engine.rs
# PROTO_BASE and holo-utils FibPolicy::proto_id (base+0 OSPF, +1 static, +2 BGP, +3 every
# other protocol holo installs) — verified against holo-utils/src/southbound.rs. Purely
# cosmetic for `ip route show`; cfab's own sweep matches on the numeric ids, not these names.
201	cfab-ospf
202	cfab-static
203	cfab-bgp
204	cfab-other
